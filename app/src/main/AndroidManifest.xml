<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jeadev.orolasa">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Account_Forms" />
        <activity android:name=".form" />
        <activity android:name=".ticket" />
        <activity android:name=".Ticket_mainBody" />
        <activity android:name=".mainpayment" />
        <activity android:name=".hourslist" />
        <activity android:name=".Banking"></activity>
    </application>

</manifest>